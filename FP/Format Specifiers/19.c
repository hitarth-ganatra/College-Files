#include <stdio.h>

void main(){
	// C Program to perform BITWISE AND, OR, XOR operations on 2 integer values
	
	int a = 12, b = 25;

	printf("Output = %d\n", a & b);
	printf("Output = %d\n", a | b);
	printf("Output = %d\n", a ^ b);
}