#include <stdio.h>

void main(){
	// C Program to take 3 numbers and print them

	int a, b, c;
	printf("Enter 3 numbers: ");
	scanf("%d %d %d", &a, &b, &c);

	printf("You entered %d, %d and %d", a, b, c);
}